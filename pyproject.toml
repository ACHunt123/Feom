[build-system]
requires = ["meson-python", "ninja"]
build-backend = "mesonpy"

[project]
name = "Feom"
version = "0.1.0"
description = "Fortran HEOM propagation"
readme = "README.md"
requires-python = ">=3.9"